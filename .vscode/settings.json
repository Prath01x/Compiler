{
    "java.test.config" : {
        "env": {
            "PATH" : "${workspaceFolder}/libs/",
            "LD_LIBRARY_PATH" : "${workspaceFolder}/libs/",
            "DYLD_LIBRARY_PATH" : "${workspaceFolder}/libs/"
        } // to link with z3
    }
}